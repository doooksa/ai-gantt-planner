# Dev leaves this empty: the Vite dev server proxies /api and /ws to :8000.
# For a deployed build, point it at the backend origin, e.g.
#   VITE_API_BASE=https://ai-gantt-api.onrender.com
VITE_API_BASE=
